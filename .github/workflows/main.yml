name: MLOps CI/CD - Build, Test, Deploy & Stress Test Pipeline

on:
  push:
    branches:
      - dev
      - main
      
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

env:
  PROJECT_ID: keen-phalanx-473718-p1
  AR_REGION: us-central1
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  AR_REPOSITORY: mlops-mlflow
  IMAGE_NAME: fastapi-backend
  GKE_CLUSTER: fastapi-ci-cluster
  GKE_ZONE: us-central1-a
  DEPLOYMENT_NAME: fastapi-backend
  MANIFEST_PATH: k8s/deployment.yaml
  
jobs:
  infrastructure-setup:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Google Cloud Authentication
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: ${{ secrets.GCP_SA }}

      - name: Create GKE Cluster (If not found)
        run: |
          echo "Checking for cluster ${{ env.GKE_CLUSTER }}..."
          if ! gcloud container clusters describe ${{ env.GKE_CLUSTER }} --zone ${{ env.GKE_ZONE }} --project ${{ env.PROJECT_ID }} --quiet; then
            echo "Cluster not found. Creating a new GKE cluster..."
            
            gcloud container clusters create ${{ env.GKE_CLUSTER }} \
              --zone ${{ env.GKE_ZONE }} \
              --num-nodes=1 \
              --machine-type=e2-small \
              --release-channel=regular \
              --project=${{ env.PROJECT_ID }}
          else
            echo "Cluster ${{ env.GKE_CLUSTER }} already exists. Skipping creation."
          fi

  ci-experiments:
    runs-on: ubuntu-latest
    needs: infrastructure-setup
    timeout-minutes: 20
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Setup CML
        uses: iterative/setup-cml@v2.0.1

      - name: Google Cloud Authentication
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: ${{ secrets.GCP_SA }}

      - name: Install DVC and dependencies
        run: |
          pip install dvc dvc-gs dvc[gcs]
          pip install -r requirements.txt

      - name: DVC Pull Data
        run: dvc pull

      - name: Run CI Experiments
        run: |
          cat > ci_experiments_report.md << 'REPORT_EOF'
          # 🔬 CI/CD Experiment Results
          
          **Pipeline Run:** `${{ github.sha }}`
          **Branch:** `${{ github.ref_name }}`
          **Triggered by:** @${{ github.actor }}
          
          ---
          
          ## 📊 Experiment Execution Log
          
          REPORT_EOF
          
          echo '```'
          python test_experiment.py 2>&1 | tee -a ci_experiments_report.md
          echo '```' >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md

      - name: Add Confusion Matrix to Report
        run: |
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "## 📈 Confusion Matrix - All Species" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          
          if [ -f "ci_artifacts/ci_confusion_matrix.png" ]; then
            echo "### Multi-Class Classification Performance" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            cml publish ci_artifacts/ci_confusion_matrix.png --md >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            echo "✅ **Confusion Matrix:** Shows classification accuracy across all 3 species (setosa, versicolor, virginica)" >> ci_experiments_report.md
          else
            echo "⚠️ Confusion matrix not found." >> ci_experiments_report.md
          fi
          echo "" >> ci_experiments_report.md

      - name: Add SHAP Explainability to Report
        run: |
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "## 🧠 SHAP Explainability Analysis - All Species" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "SHAP (SHapley Additive exPlanations) shows feature importance for each species." >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          
          for species in setosa versicolor virginica; do
            echo "### 🌸 Species: **${species^}**" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            
            if [ -f "ci_artifacts/ci_shap_waterfall_${species}.png" ]; then
              echo "#### Waterfall Plot (Single Prediction)" >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
              cml publish "ci_artifacts/ci_shap_waterfall_${species}.png" --md >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
              echo "Shows how each feature contributes to pushing the prediction from base value." >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
            fi
            
            if [ -f "ci_artifacts/ci_shap_beeswarm_${species}.png" ]; then
              echo "#### Beeswarm Plot (Global Importance)" >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
              cml publish "ci_artifacts/ci_shap_beeswarm_${species}.png" --md >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
              echo "Feature impact across all samples (red=high, blue=low)." >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
            fi
            
            if [ -f "ci_artifacts/ci_shap_bar_${species}.png" ]; then
              echo "#### Bar Plot (Average Importance)" >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
              cml publish "ci_artifacts/ci_shap_bar_${species}.png" --md >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
              echo "Mean absolute SHAP values - most important features for predicting this species." >> ci_experiments_report.md
              echo "" >> ci_experiments_report.md
            fi
            
            echo "---" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
          done

      - name: Add Data Drift Analysis to Report
        run: |
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "## 📉 Data Drift Detection (Evidently AI)" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "Compares reference data (training) with current data (test) to identify distribution changes." >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          
          if [ -f "ci_artifacts/ci_drift_report.html" ]; then
            echo "### Drift Report Summary" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            echo "✅ Drift analysis completed successfully" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            echo "**Key Metrics:**" >> ci_experiments_report.md
            echo "- Reference dataset: Training data" >> ci_experiments_report.md
            echo "- Current dataset: Test data" >> ci_experiments_report.md
            echo "- Features analyzed: sepal_length, sepal_width, petal_length, petal_width" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            echo "📄 Full HTML report available in workflow artifacts" >> ci_experiments_report.md
          else
            echo "⚠️ Drift report not generated" >> ci_experiments_report.md
          fi
          echo "" >> ci_experiments_report.md

      - name: Add Fairness Metrics to Report
        run: |
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "## ⚖️ Fairness Analysis" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "Evaluates model performance across different demographic groups (location attribute)." >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          
          # Extract fairness metrics if available
          if grep -q "Fairness Accuracy Difference:" ci_experiments_report.md 2>/dev/null; then
            fairness_diff=$(grep "Fairness Accuracy Difference:" ci_experiments_report.md | grep -oP '[\d.]+' | head -1)
            echo "### Fairness Metrics Summary" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            echo "- **Accuracy Difference Between Groups:** ${fairness_diff}" >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            
            if (( $(echo "$fairness_diff < 0.05" | bc -l) )); then
              echo "✅ **Status:** Minimal bias (difference < 0.05)" >> ci_experiments_report.md
            elif (( $(echo "$fairness_diff < 0.10" | bc -l) )); then
              echo "⚠️ **Status:** Moderate bias detected (0.05 ≤ difference < 0.10)" >> ci_experiments_report.md
            else
              echo "❌ **Status:** Significant bias detected (difference ≥ 0.10)" >> ci_experiments_report.md
            fi
          else
            echo "Fairness metrics will be displayed after experiment completion." >> ci_experiments_report.md
          fi
          echo "" >> ci_experiments_report.md

      - name: Add Performance Summary
        run: |
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "## 📊 Model Performance Summary" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "| Species | Precision | Recall | F1-Score |" >> ci_experiments_report.md
          echo "|---------|-----------|--------|----------|" >> ci_experiments_report.md
          
          # Try to extract metrics
          if grep -q "setosa: P=" ci_experiments_report.md 2>/dev/null; then
            for species in setosa versicolor virginica; do
              line=$(grep "${species}: P=" ci_experiments_report.md | head -1)
              if [ ! -z "$line" ]; then
                precision=$(echo "$line" | grep -oP 'P=\K[\d.]+')
                recall=$(echo "$line" | grep -oP 'R=\K[\d.]+')
                f1=$(echo "$line" | grep -oP 'F1=\K[\d.]+')
                echo "| **${species^}** | ${precision} | ${recall} | ${f1} |" >> ci_experiments_report.md
              fi
            done
          else
            echo "| Pending | - | - | - |" >> ci_experiments_report.md
          fi
          
          echo "" >> ci_experiments_report.md

      - name: Add Artifacts Summary
        run: |
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "## 📦 Generated Artifacts" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          
          if [ -d "ci_artifacts" ]; then
            echo "### Files Generated:" >> ci_experiments_report.md
            echo '```
            ls -lh ci_artifacts/ >> ci_experiments_report.md
            echo '```' >> ci_experiments_report.md
            echo "" >> ci_experiments_report.md
            
            echo "### Artifact Categories:" >> ci_experiments_report.md
            echo "- **SHAP Plots:** Waterfall, Beeswarm, and Bar charts for each species" >> ci_experiments_report.md
            echo "- **Confusion Matrix:** Multi-class classification performance" >> ci_experiments_report.md
            echo "- **Drift Reports:** HTML reports from Evidently AI" >> ci_experiments_report.md
            echo "- **Model Artifacts:** Logged to MLflow" >> ci_experiments_report.md
          fi
          echo "" >> ci_experiments_report.md

      - name: Add MLflow Tracking Info
        run: |
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "## 🔬 MLflow Experiment Tracking" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "All experiments tracked in MLflow for reproducibility and comparison." >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "**MLflow UI:** [View Experiments](http://34.71.75.36:5000)" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "**Experiment Name:** \`ci-experiment\`" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "### Logged Information:" >> ci_experiments_report.md
          echo "- ✅ Model hyperparameters" >> ci_experiments_report.md
          echo "- ✅ Training and validation metrics" >> ci_experiments_report.md
          echo "- ✅ All visualization artifacts (SHAP, confusion matrix)" >> ci_experiments_report.md
          echo "- ✅ Fairness and drift detection results" >> ci_experiments_report.md
          echo "- ✅ Model binaries for deployment" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "---" >> ci_experiments_report.md
          echo "" >> ci_experiments_report.md
          echo "🤖 *Generated by CI/CD Pipeline* | 📅 $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> ci_experiments_report.md

      - name: Upload CI Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ci-experiment-artifacts
          path: ci_artifacts/
          retention-days: 30

      - name: Create CML Report Comment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: cml comment create ci_experiments_report.md

  build-and-deploy:
    runs-on: ubuntu-latest
    needs: ci-experiments
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Setup CML
        uses: iterative/setup-cml@v2.0.1

      - name: Install DVC
        run: pip install dvc dvc-gs dvc[gcs]
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Google Cloud Authentication
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: ${{ secrets.GCP_SA }}
          
      - name: DVC PULL
        run: dvc pull

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.AR_REGION }}-docker.pkg.dev
        
      - name: Build and Push Docker Image
        run: |
          FULL_IMAGE_TAG="${{ env.AR_REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPOSITORY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          LATEST_TAG="${{ env.AR_REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.AR_REPOSITORY }}/${{ env.IMAGE_NAME }}:latest"
          
          docker build -t "${FULL_IMAGE_TAG}" -t "${LATEST_TAG}" .
          docker push "${FULL_IMAGE_TAG}"
          docker push "${LATEST_TAG}"
          
      - name: Get GKE Credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: ${{ env.GKE_CLUSTER }}
          location: ${{ env.GKE_ZONE }}

      - name: Create Kubernetes Secret for GCP Service Account
        run: |
          kubectl create secret generic gcp-sa-secret \
            --from-literal=gcp-sa.json='${{ secrets.GCP_SA }}' \
            --dry-run=client -o yaml | kubectl apply -f -
          
          echo "✓ GCP Service Account secret created/updated"

      - name: Wait for API Server Stability
        run: sleep 60

      - name: Apply Kubernetes Manifests
        run: kubectl apply -f ${{ env.MANIFEST_PATH }}
          
      - name: Wait for Deployment Readiness
        run: sleep 120

      - name: Verify Deployment Rollout
        run: |
          kubectl rollout status deployment/${{ env.DEPLOYMENT_NAME }} --timeout=5m
          echo "Deployment successful!"
          
      - name: Wait for LoadBalancer IP
        run: sleep 120

      - name: Show Service Status
        run: |
          kubectl get service ${{ env.DEPLOYMENT_NAME }}-service
          kubectl get nodes -o wide

  stress-test:
    runs-on: ubuntu-latest
    needs: build-and-deploy
    timeout-minutes: 15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup CML
        uses: iterative/setup-cml@v2.0.1
        
      - name: Google Cloud Authentication
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: ${{ secrets.GCP_SA }}

      - name: Get GKE Credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: ${{ env.GKE_CLUSTER }}
          location: ${{ env.GKE_ZONE }}

      - name: Install wrk
        run: sudo apt-get update && sudo apt-get install -y wrk

      - name: Get Service External IP
        id: get_ip
        run: |
          EXTERNAL_IP=$(kubectl get svc ${{ env.DEPLOYMENT_NAME }}-service -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
          echo "external_ip=$EXTERNAL_IP" >> $GITHUB_OUTPUT
          echo "External IP: $EXTERNAL_IP"

      - name: Create wrk Lua Script
        run: |
          cat > iris_payload.lua << 'LUA_EOF'
          local post_body = '{"sepal_length": 5.1, "sepal_width": 3.5, "petal_length": 1.4, "petal_width": 0.2}'
          
          request = function()
            return wrk.format("POST", nil, {["Content-Type"] = "application/json"}, post_body)
          end
          LUA_EOF
          
      - name: Warm-up Endpoints
        run: |
          EXTERNAL_URL="http://${{ steps.get_ip.outputs.external_ip }}"
          PAYLOAD='{"sepal_length": 5.1, "sepal_width": 3.5, "petal_length": 1.4, "petal_width": 0.2}'
          
          curl -s "${EXTERNAL_URL}/health" > /dev/null 2>&1
          curl -s -X POST -H "Content-Type: application/json" -d "$PAYLOAD" "${EXTERNAL_URL}/predict" > /dev/null 2>&1

      - name: "Scenario 1: HPA Scaling Test"
        run: |
          EXTERNAL_URL="http://${{ steps.get_ip.outputs.external_ip }}"
          TARGET="${EXTERNAL_URL}/predict"
          
          cat > stress_report.md << 'STRESS_EOF'
          # 🚀 Stress Test & HPA Results
          
          STRESS_EOF
          
          echo "**External IP:** ${{ steps.get_ip.outputs.external_ip }}" >> stress_report.md
          echo "**Test Date:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> stress_report.md
          echo "" >> stress_report.md
          echo "---" >> stress_report.md
          echo "" >> stress_report.md
          echo "## SCENARIO 1: HPA Scaling on \`/predict\` (Max Pods: 3, 100 Users)" >> stress_report.md
          echo "" >> stress_report.md
          
          kubectl patch hpa/fastapi-backend-hpa -p '{"spec":{"maxReplicas": 3}}'
          echo "Starting 30-second stress test with 100 concurrent connections..." >> stress_report.md
          echo "" >> stress_report.md
          
          wrk -t4 -c100 -d30s --script ./iris_payload.lua $TARGET > wrk_results_1.txt
          
          echo "### Test 1 Results:" >> stress_report.md
          echo '```
          cat wrk_results_1.txt >> stress_report.md
          echo '```' >> stress_report.md
          
          echo "" >> stress_report.md
          echo "Waiting 60s to observe scaling..." >> stress_report.md
          sleep 60
          
          echo "" >> stress_report.md
          echo "### Kubernetes Status After Scaling:" >> stress_report.md
          echo '```
          kubectl get deployment fastapi-backend -o wide >> stress_report.md
          kubectl get hpa fastapi-backend-hpa -o wide >> stress_report.md
          echo '```' >> stress_report.md
          
      - name: "Scenario 2: Bottleneck Test (C: 1000)"
        run: |
          EXTERNAL_URL="http://${{ steps.get_ip.outputs.external_ip }}"
          TARGET="${EXTERNAL_URL}/health"
          
          echo "" >> stress_report.md
          echo "---" >> stress_report.md
          echo "## SCENARIO 2: Bottleneck (Max Pods: 1, C: 1000) on \`/health\`" >> stress_report.md
          echo "" >> stress_report.md
          
          kubectl scale deployment/fastapi-backend --replicas=1
          kubectl patch hpa/fastapi-backend-hpa -p '{"spec":{"maxReplicas": 1}}'
          echo "HPA restricted to maxReplicas=1. Starting 120-second test..." >> stress_report.md
          echo "" >> stress_report.md
          sleep 30
          
          wrk -t4 -c1000 -d120s $TARGET > wrk_results_2.txt
          
          echo "### Test 2 Results:" >> stress_report.md
          echo '```
          cat wrk_results_2.txt >> stress_report.md
          echo '```' >> stress_report.md
          
          echo "" >> stress_report.md
          echo "### Kubernetes Status After Test:" >> stress_report.md
          echo '```
          kubectl get deployment fastapi-backend -o wide >> stress_report.md
          kubectl get hpa fastapi-backend-hpa -o wide >> stress_report.md
          echo '```' >> stress_report.md

      - name: "Scenario 3: Severe Bottleneck (C: 2000)"
        run: |
          EXTERNAL_URL="http://${{ steps.get_ip.outputs.external_ip }}"
          TARGET="${EXTERNAL_URL}/health"
          
          echo "" >> stress_report.md
          echo "---" >> stress_report.md
          echo "## SCENARIO 3: Severe Bottleneck (Max Pods: 1, C: 2000)" >> stress_report.md
          echo "" >> stress_report.md
          echo "Testing with **extreme** concurrency (2000 connections) for 60 seconds." >> stress_report.md
          echo "" >> stress_report.md
          
          wrk -t4 -c2000 -d60s $TARGET > wrk_results_3.txt
          
          echo "### Test 3 Results:" >> stress_report.md
          echo '```
          cat wrk_results_3.txt >> stress_report.md
          echo '```' >> stress_report.md
          
          echo "" >> stress_report.md
          echo "### Kubernetes Status After Test:" >> stress_report.md
          echo '```
          kubectl get deployment fastapi-backend -o wide >> stress_report.md
          kubectl get hpa fastapi-backend-hpa -o wide >> stress_report.md
          echo '```' >> stress_report.md
          echo "" >> stress_report.md
          echo "---" >> stress_report.md
          echo "" >> stress_report.md
          echo "🤖 *Generated by CI/CD Pipeline* | 📅 $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> stress_report.md

      - name: Create CML Stress Test Report
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: cml comment create stress_report.md

